# Start
    
    docker-compose up -d

then visit http://0.0.0.0:10080/

# Install

    DB_TYPE      = mysql
    HOST         = mysql:3306
    NAME         = gogs
    USER         = root
    PASSWD       = root
    ROOT_URL     = http://localhost:10080/
